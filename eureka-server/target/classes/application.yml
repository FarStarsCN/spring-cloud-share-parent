server:
  port: 8081

eureka:
  instance:
    hostname: localhost
  client:
    # �Ƿ�������ķ�������ͬ�������б�
    fetch-registry: false
    # eureka�������Լ�Ҳ����һ��client����������Ҫ������һ��server
    register-with-eureka: false
    service-url:
      # ����ط��Ǹ���ӣ�����д��default-zone, Ҫ���շ�ʽ
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

spring:
  application:
    name: eureka-server