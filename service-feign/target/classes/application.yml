server:
  port: 8092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/

spring:
  application:
    name: service-feign

# Ĭ��false������������۶ϻ���
feign:
  hystrix:
    enabled: true